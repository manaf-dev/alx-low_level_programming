#include <stdio.h>
/**
 * main - outputs base 10 single numbers
 * Return: 0 is returned
 *
 * Destription: print single numbers from 0
 */
int main(void)
{
	int num;
	for (num = 0; num < 10; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
